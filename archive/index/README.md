# archive/index

Capability archive index root.

This directory is intentionally tracked so archive extraction workflows always
have a stable on-disk target, even on fresh checkouts.

- Add retrieval-ready packets and index manifests here.
- Keep artifacts deterministic and referenceable from job/procurement outputs.
